# Order Parser Outputs

This directory contains JSON files generated by the batch processing script.

## Usage

Run the batch processor:

```bash
cd backend
python -m scripts.run_on_dataset --zip ../data/estilos_pedidos.zip --output ../outputs
```

## Files

- `*.json` - Individual order parsing results
- `_stats.json` - Batch processing statistics
